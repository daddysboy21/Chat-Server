{
  "dependencies": {
    "express": "^4.18.2",
    "socket.io": "^4.7.3"
  },
  "scripts": {
    "start": "node server.js",
    "build": "npm install && npm run migrate && npm run build",
    "test": "jest"
  }
}
